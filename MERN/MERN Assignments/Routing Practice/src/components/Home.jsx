// The simplest possible component: no props, no state, no hooks.
// It just returns some JSX. Requirement #1 of the assignment.
const Home = () => {
  return <h1>Welcome Home</h1>;
};

// Default export so App.jsx can import it under any name.
export default Home;