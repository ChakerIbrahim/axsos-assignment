# Connects our application to the MySQL schema for this project
spring.datasource.url=jdbc:mysql://localhost:3306/authentication
spring.datasource.username=root
spring.datasource.password=root
# Lets Hibernate create/update the users table automatically from our model
spring.jpa.hibernate.ddl-auto=update
# Tells Spring where to find our JSP view files
spring.mvc.view.prefix=/WEB-INF/
